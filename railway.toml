[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'exec uvicorn server.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
